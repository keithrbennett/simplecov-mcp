# Test Example Code

Run all the command examples in README.md, docs/user/*.md, and docs/dev/*.md, but *not* docs/dev/arch-decisions/*.md 
and not docs/dev/presentations/*.md files and make sure they work as intended.
